const localServer = "http://local-server/";
const GOOGLE_ANALITICS = `${localServer}google-analytics`;
const GOOGLE_ADS = `${localServer}google-ads`;
const META_ADS = `${localServer}meta-ads`;
const CRM_DATA = `${localServer}crm-data`;

export { GOOGLE_ANALITICS, GOOGLE_ADS, META_ADS, CRM_DATA };
